/**
 * Lista de edades de usuarios. Arreglo genérico, se puede asignar cualquier tipo de dato
 * @type {Array}
 */
const age = [19, 20, 30, 100, {}, true]

/**
 * Array of User's Scores. Arreglo específico de números
 * @type {Array<Number>}
 */
const scores = [10, 199.99, 299.99, 3000]

/**
 * Array of Number and strings
 * @type {Array<Number | string>}
 */
const scoresAndString = [10, "20", "30", 3000]